<view class="login-page">
  <view class="login-body">
    <view class="login-heading">
      <text class="h1">欢迎来到24点挑战</text>
    </view>
    <view class="login-card card section">
      <text class="login-ill">🃏</text>
      <text class="login-text">为了同步您的游戏数据，请使用微信登录。</text>

      <!-- 微信开放能力，需用 button 组件 -->
      <button
        class="login-btn highlight"
        wx:if="{{!isLoggedIn}}"
        open-type="getUserInfo"
        bind:getuserinfo="onGetUserInfo"
      >
        微信授权登录
      </button>

      <view wx:if="{{isLoggedIn}}" class="login-success">
        <text>登录成功！正在跳转...</text>
      </view>
    </view>

    <view class="tips">
      <text>登录即代表您同意我们的用户协议和隐私政策。</text>
    </view>
  </view>
</view>
